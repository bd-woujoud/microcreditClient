
const port = 5000
const serverurl = 'http://localhost:' + port

export const requests = {
    demandeapi: serverurl + '/demande',
    usersapi: serverurl + '/user',
    clientapi: serverurl + '/client',
}